@startuml
title 经济模型闭环活动图

start
:算力提供者调用 `registerMiner()` 并质押代币;
note right: 加入网络成为可信节点

:用户调用 `infer()` 并支付费用;
note left: 发布推理任务

:PromptScheduler 根据矿工状态分配任务;

:矿工监听到 `TaskAssigned` 事件;
note right: 获取任务详情

:矿工执行计算;

:矿工调用 `submitSolution()` 提交结果;

if (结果验证成功?) then (yes)
  :合约自动将费用分配给矿工;
  note left: 激励结算
else (no)
  :处理失败情况 (例如：惩罚);
endif

stop
@enduml
