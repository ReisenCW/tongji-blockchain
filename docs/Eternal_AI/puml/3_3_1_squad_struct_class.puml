@startuml
skinparam class {
    BackgroundColor LightBlue
    ArrowColor SteelBlue
    BorderColor SteelBlue
}
hide empty members

class Squad {
    + owner: address
    + leaderAgentId: uint256
    + isMember: mapping(uint256 => bool)
    + shares: mapping(uint256 => uint256)
}

note right of Squad
  **Squad 结构体**
  定义了一个链上协作小队，
  包含领导者、成员列表
  和收益分配比例。
end note
@enduml
